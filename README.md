# Try to use meteor packages for Uploadcare

## Settings

Copy file `settings.json.default` and rename to `settings.json`.

Set in `settings.json` your public key for Uploadcare instead `demopublickey`.

```json
"uploadcare": {
  "publicKey": "YOUR_PUBLIC_KEY"
}
```